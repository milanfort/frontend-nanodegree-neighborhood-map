'use strict';

var _ = require('lodash');

var util = module.exports = {

    first: function (x) {
        return _.first(x);
    },

    capitalize: function (x) {
        return _.capitalize(x);
    }
};
